git push -u origin learning-rebase
#这个就能push的时候直接关联远程分支了
#-u Is the alias(?) for --set-upstream

git <command> -h 
#print info for command

git push origin --delete <remoteBranchName>
#This command can delete branch from remote repository, just like deleting branches using the UI on github.com

git checkout <commit>
#This will make you into a detached mode. 
